var comment_error = document.getElementById('comment_error');
var comment_button = document.getElementById('comment_button');

comment_error.insertAdjacentHTML('afterbegin',"<%= j(render 'shared/error_messages',object:@comment) %>");
comment_button.addEventListener('click',()=>{
  comment_error.textContent='';
})
