<% if user_avatar.present? %>
    <%= link_to image_tag("/uploads/user/avatar/#{user_id}/#{user_avatar}", class: "circle small"), user_path(user_id) %>
  <% else %>
    <%=link_to image_tag('no-image.jpg', class: "circle small"), user_path(user_id) %>
  <% end %>

