
<% if user.avatar? %>
<%= link_to image_tag(user.avatar.url, class: "circle big"), user_path(user) %>
<% else %>
<%=link_to image_tag('no-image.jpg', class: "circle big"), user_path(user) %>
<% end %>


  <h5><%= link_to user.name,user %></h5>

<div>
  <% if user.coach? %>
  <i class="far fa-check-circle" style="color:lightgreen"></i>
  <span style="color:lightgreen">コーチ</span>
  <% end %>
</div>