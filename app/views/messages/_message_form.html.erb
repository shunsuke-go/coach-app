<%= form_with(model:@message, local: true) do |f| %> </p>
<%= f.label :content, "メッセージ" %>
<%= f.text_area :content, class:'form-control' %>
<%= f.submit "送信",class:"btn btn-primary"%>
<%= hidden_field_tag :user_id,@user[0].id %>
<%= hidden_field_tag :room_id,@room.id %>
<% end %>
