<% if @articles.any? %>
  <div class="row">
    <div class="col s12 m8 push-m2">
      <h4>いいねした記事一覧</h4>
      <ul class="articles">
        <%= render @articles %>
        <div class="center"><%= will_paginate @articles %></div>
      </ul>
    </div>
  </div>
<% end %>
