var unlike = document.getElementById('unlike_<%= @article.id %>');
var like_form = document.getElementById('like_form_<%= @article.id %>');
var likes_count = document.getElementById('likes_count_<%= @article.id %>');

like_form.innerHTML="<%= j(render 'like_button', article: @article) %>";

fetch('/articles/<%= @article.id %>/likes/counts.json',{
  mode: 'cors',
  method: 'GET',
  headers: {
    Authorization: "Token <%= current_user.token %>",
    "Content-Type": "application/json"
  }
}).then(response => response.json())
.then(data => {
likes_count.textContent=data.counts
}).catch(e => {
  console.log(e);
})